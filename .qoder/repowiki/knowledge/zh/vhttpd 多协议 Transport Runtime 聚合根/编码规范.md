- 所有子模块以 `src/<pkg>/` 目录组织，通过 V 包名直接 import 而非相对路径，跨模块边界使用显式包名。
- 测试文件统一命名为 `*_test.v`，由 Makefile 自动 discover，重型/in-proc/db 测试通过前缀或目标名区分。
- 运行时能力以 `<feature>_runtime.v` 命名并导出 `pub fn` 组装函数，由 `main.v` 或各子系统 builder 在启动期组合。
- 配置采用 TOML + CLI 覆盖模式，变量展开支持 `${section.key}` 与 `${env.NAME:-default}` 语法。