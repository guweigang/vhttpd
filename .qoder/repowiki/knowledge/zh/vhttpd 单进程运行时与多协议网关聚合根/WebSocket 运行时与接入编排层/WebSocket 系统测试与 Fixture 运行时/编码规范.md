- 每个文件均为独立可执行的 `module main`，以 `fn test_` 前缀命名测试函数，使用 `assert` 直接断言而非封装测试框架。
- 构造被测对象时使用最小化内联结构体（如 `WebSocketRuntime.new(true)`、`UpstreamRuntimeRegistry.new()`），避免额外 mock 层。
- 对可能返回错误的 API 调用统一采用 `or { panic(...) }` 或 `or { assert true; return }` 模式在测试中短路失败路径。
- 对连接生命周期与 hub 状态的验证遵循「先构造初始状态 → 调用目标方法 → 逐一断言状态字段」的三段式写法。