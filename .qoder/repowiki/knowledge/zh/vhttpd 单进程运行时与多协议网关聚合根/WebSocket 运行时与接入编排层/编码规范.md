- 所有对外暴露的 WebSocket 能力均通过 `ws.RuntimeContext` 方法访问，禁止直接操作 HubState
- Worker↔Kernel 通信统一使用 `transport.WorkerWebSocketFrame` 结构体，event 字段取值限定为 open/message/close/done/error/accept
- 会话生命周期事件经 `websocket_*_exchange` 函数投影为 `dispatch.Exchange`，并携带 `websocket.source`/`websocket.event` 等元数据