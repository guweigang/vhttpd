- 对外暴露的运行时方法一律以 `app.providers.xxx()` 形式透传至 `ProviderRuntimeHub`，由 Hub 再分派给具体 provider 或上游状态。
- Provider 名称使用 `provider.ProviderName.xxx()` 常量而非硬编码字符串，确保注册表查询与匹配一致。
- 运行时配置通过 `ProviderRuntimeSettings` 一次性注入 Hub，再由 `apply_provider_runtime_settings` 批量回填到已注册 spec 的 runtime_driver 字段。