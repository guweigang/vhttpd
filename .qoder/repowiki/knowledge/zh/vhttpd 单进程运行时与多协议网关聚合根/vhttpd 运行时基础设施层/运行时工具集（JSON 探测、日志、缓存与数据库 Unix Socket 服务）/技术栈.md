V 语言原生 `net.unix` Unix Domain Socket 作为进程内 IPC 通道；dbx 在 `enable_db=true` 时链接 MySQL C 客户端（`db.mysql` 底层）与 PostgreSQL 驱动（`db.pg`），并通过 `C.mysql_fetch_*` 直接访问 libmysqlclient 获取字段元数据；`sync.Mutex` 保护 cachex/dbx Runtime 的共享状态。