根级 Makefile 是唯一入口：`make deps-core|deps-vjsx|deps-db|deps-full` 安装依赖，`make doctor` 校验环境，`make build|prod` 编译 vhttpd 二进制，`make test-fast|test-inproc|test-codexbot|test-all` 运行 V 测试，`make demo-vslim|demo-ai|demo-symfony|demo-laravel|demo-wordpress` 启动示例，`make package-wp-plugin` 打包 WordPress 插件。