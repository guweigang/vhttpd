- 诊断以 `runtime_plan.PlanDiagnostic` 结构返回，统一包含 severity/code/path/message 四字段，code 采用 `pipeline_*_missing_*` / `pipeline_*_capability_mismatch` 前缀命名。
- 能力不匹配同时产出两类产物：字符串数组（`[]string`，形如 `pipeline_capability_mismatch:<id>:<ingress>:<capability>`）用于快速判断，以及 `PipelineCapabilityIssue` 列表供上层生成结构化诊断。
- 跨资源引用检查通过 helper 函数按 `ResourceRef.domain` 做 match 分派（`.listener/.adapter/.relay/.provider/.transform/.terminal`），新增 domain 时只需扩展该分派分支。
- 测试用例以“构造最小 RuntimePlan → 调用入口 → assert 诊断 code/path/message”三段式组织，并用 `.any(it.code == ...)` 定位特定诊断而非依赖顺序。