- 对外暴露的函数统一以 `(mut app App)` 接收可变应用实例，并通过 `app.emit` 输出指标事件，错误路径也走 emit 上报失败原因。
- 字符串字段在落盘前一律 `trim_space()`，空串场景使用 fallback 值（如 request_id、trace_id、name）保证下游一致性。
- 资源标识采用规范化函数处理：`normalize_event_ingress_ref` 为裸名补全 `adapter:` 前缀，`normalize_pipeline_id` 剥离 `pipeline:` 前缀，避免调用方重复做格式校验。