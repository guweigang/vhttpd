推荐通过 `make build vhttpd` 编译后以 `./vhttpd --config examples/config/<xxx>.toml` 启动；各子项目有独立命令：`cd examples/codexbot-app && composer install && composer test`；`codexbot-app-ts` 需设置 `FEISHU_APP_ID/SECRET/VERIFICATION_TOKEN/ENCRYPT_KEY` 环境变量后以 `--config codexbot-app-ts/codexbot.toml` 启动；`wordpress` 需要 `VPHP_WP_ROOT=/abs/path/to/wordpress`。