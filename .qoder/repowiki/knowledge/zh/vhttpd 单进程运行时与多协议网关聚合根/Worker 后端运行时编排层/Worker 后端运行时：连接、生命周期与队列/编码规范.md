- 对共享状态 `ws.worker_backend.*` 的访问统一通过 `ws.mu.@lock()` / `ws.mu.unlock()` 保护，并在函数入口/退出处成对出现，避免遗漏。
- 对外暴露的错误语义化：选择失败统一返回包含 `'worker not configured'` / `'all workers busy'` / `'worker queue full'` / `'worker queue timeout'` 等固定前缀的错误字符串，供上层 `or { ... }` 分支识别。
- 关键生命周期变更通过 `port.emit_fn('worker.*', {...})` 上报结构化事件，字段名采用 snake_case 并附带 `worker_id`、`socket`、`reason` 等诊断信息。
- 需要重试的逻辑采用 `for attempt in 0 .. 10` 循环 + `time.sleep(10 * time.millisecond)` 退避的模式，并将最后一次错误保存在 `last_err` 变量中。