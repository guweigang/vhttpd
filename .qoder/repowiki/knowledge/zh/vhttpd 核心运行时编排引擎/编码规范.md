- 采用 `unsafe` 指针转换配合 `voidptr` 存储实现接口适配，以规避编译器在复杂闭包场景下的代码生成限制。
- 所有跨模块状态访问均通过显式的 `mut app App` 引用或 `AppFacade` 接口进行，严禁在子模块中直接引用全局变量。
- 使用 `runtime_trace` 函数向 `/tmp/vhttpd_runtime_trace.log` 写入 JSON 格式的时序日志，用于全链路性能诊断。