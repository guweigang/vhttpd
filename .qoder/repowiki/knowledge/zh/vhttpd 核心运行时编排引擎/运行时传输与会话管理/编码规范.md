- 所有跨进程通信帧均采用“4字节网络字节序长度头 + JSON 载荷体”的二进制封装格式。
- 会话句柄（SessionHandle）通过静态工厂方法（如 `from_stream_dispatch`）从各类原始请求结构中派生，以统一初始化追踪与路由元数据。
- 工作进程管理采用“预建槽位（build_managed_worker_slot）+ 异步启动（start_managed_worker）”的两阶段初始化模式，以支持并发池化。