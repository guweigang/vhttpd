- 采用 Facade 模式隔离核心逻辑与底层实现，例如 `AppFacade` 接口屏蔽了具体的 Worker 后端细节。
- 所有共享状态访问均遵循严格的锁顺序（app.mu > feishu.mu > ws_hub.mu...）以防止死锁。
- 配置合并采用显式的 `merge_*_config` 函数族，确保默认值、全局配置和局部覆盖按优先级正确生效。
- 执行器生命周期通过接口 `LogicExecutorLifecycle` 统一抽象，支持 `prepare_bootstrap`、`start` 和 `stop` 的标准流程。