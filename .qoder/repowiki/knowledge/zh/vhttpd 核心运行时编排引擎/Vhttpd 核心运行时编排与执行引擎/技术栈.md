- **语言与运行时**：V 语言 (Vlang)，利用其内置的 `veb` Web 框架和 `vjsx` 模块进行 JavaScript/TypeScript 嵌入式执行。
- **进程管理**：通过 `os.Process` 直接管理 PHP Worker 进程池，使用 Unix Domain Sockets 进行高性能 IPC。
- **并发模型**：基于 V 语言的协程 (`spawn`) 和通道 (`chan`) 实现非阻塞 I/O 和任务分发。