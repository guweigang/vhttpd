- **自定义二进制协议**：实现了基于 Protobuf Varint 编码的 `FeishuRuntimeProtoFrame` 编解码器，用于飞书 WebSocket 通信。
- **加密支持**：使用 AES-CBC 和 SHA256 处理飞书回调的签名验证与 payload 解密 (`feishu_runtime_callback_signature_valid`, `feishu_runtime_callback_decrypt_payload`)。