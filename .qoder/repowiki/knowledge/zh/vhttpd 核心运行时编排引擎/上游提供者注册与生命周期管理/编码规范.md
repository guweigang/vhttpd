- 所有提供者适配器均实现统一的 `Provider` 接口，确保生命周期方法签名一致。
- 使用 `NoopProviderCommandHandler` 和 `NoopProviderRuntime` 作为默认空实现，简化可选组件的集成。
- 提供者配置解析采用“命令行参数优先，配置文件兜底”的策略，并在 `provider_config.v` 中集中处理。
- 运行时状态快照统一返回 JSON 字符串，便于管理界面直接展示且解耦内部结构。