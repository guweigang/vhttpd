- 所有命令处理器均实现统一的 `execute` 签名，返回 `(bool, string)` 元组以标识是否已处理及错误信息。
- 指令标准化过程中优先使用显式声明的字段，若缺失则根据指令类型前缀或元数据进行推断（如 provider 和 event）。
- 执行结果通过 `WebSocketUpstreamCommandActivity` 快照记录，包含状态（status）、错误信息及关联的业务 ID。