- 所有 OpenAI 相关的内部函数均采用 `openai_` 前缀命名，以明确模块边界。
- 流式响应处理统一采用 `takeover_conn_reusable` 接管底层 TCP 连接，并配合 `write_chunk` 实现 SSE 协议的实时推送。
- 错误响应严格遵循 OpenAI 规范，封装为包含 `error.message`、`error.type` 和 `error.code` 字段的 JSON 结构。
- 上游调用计划（Plan）的生成支持“插件优先”策略，若插件返回 `not_handled` 则自动降级至内置路由配置。