- 采用“状态检查-加锁-执行-解锁”的模式处理共享状态，如 `ws_dispatch_conn_phase` 中对 `sync.Mutex` 的使用。
- 在 WebSocket 消息处理中，对非文本/二进制帧（如 ping/pong）直接返回不支持或关闭连接，保持协议处理的纯粹性。
- 上游连接断开后采用固定或配置化的延迟（`reconnect_delay_ms`）进行无限循环重试，确保服务的高可用性。