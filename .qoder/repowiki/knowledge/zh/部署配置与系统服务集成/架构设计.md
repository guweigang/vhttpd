- 部署层：`deploy/` 包含针对 systemd (`vhttpd@.service`) 和 launchd (`io.guweigang.vhttpd.plist`) 的服务定义，统一通过 `VHTTPD_CONFIG` 环境变量注入配置文件路径。
- 配置层：`config/` 提供标准化的 TOML 示例，涵盖单实例 PHP、多站点混合（PHP/VJSX）及纯 VJSX 场景。
- 架构边界：服务单元负责进程生命周期管理、资源限制（如文件句柄数）及安全隔离；TOML 模板定义了应用的路径映射、执行器选择（php/vjsx）及外部集成（Feishu/Codex）。