安装后通过 `composer require vphpext/vhttpd` 获取；独立运行 worker：`vendor/bin/vphp-worker --socket /tmp/vhttpd.sock -a app.php`；向 worker 发送调试帧：`vendor/bin/vphp-worker-client --socket /tmp/vhttpd.sock`；环境变量 `VHTTPD_APP`、`VHTTPD_WORKER_SOCKET`、`VHTTPD_PARENT_PID` 控制行为。