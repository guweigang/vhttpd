- 所有对外可观察的错误路径统一通过 `port.emit_fn('worker.*', fields)` 上报事件，字段包含 kind、error 与 diagnostics_json。
- 共享状态集中在 `worker.WorkerState` 上，并发访问一律以 `ws.mu.@lock()`/`unlock()` 保护，并提供 `_unlocked` 后缀的内部辅助函数。
- 需要重试/退避的 I/O 操作采用固定次数 for 循环 + `time.sleep(10ms)` 的模式，避免使用外部调度器。