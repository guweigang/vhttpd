- 采用工厂方法模式（Factory）统一创建协议对象，如 `Feishu\Command\Factory` 和 `Feishu\Event\Factory`。
- 应用处理器通常实现为返回包含特定键（如 `http`, `websocket_upstream`）的关联数组，以支持多路复用调度。
- 流式响应统一通过 `StreamResponse` 或其变体封装，支持 SSE 和纯文本分块输出。