- 对外暴露的函数以 `worker_stream_*` 前缀命名，参数统一采用结构体 + fallback 字符串的形式，便于扩展且保持调用点简洁。
- 可选字段采用 if 表达式做空值回退（status > 0、stream_type != ''、content_type != ''），再落到默认常量，避免多层 nil 检查。
- 测试函数以 `test_` 开头，先构造输入帧，再对 outcome 的 kind/status/target/headers/metadata 逐项 assert，每个用例只覆盖一条分支路径。