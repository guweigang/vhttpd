- 所有访问 `WorkerState.managed_workers` 或 `sockets` 的代码都显式持有 `ws.mu.@lock()`，并在 defer 中 `unlock()`，避免共享状态竞态。
- 对外暴露的 Engine 级方法统一返回 `EngineDrainStatus` 结构体，字段名保持 snake_case 并通过 `app.emit` 以字符串形式序列化到事件 payload。
- 诊断探针采用“先读锁复制快照，再释放锁后逐个探测”的模式，减少持锁时间并避免阻塞其他 goroutine。