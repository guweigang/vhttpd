- 对外暴露的中间数据结构使用 `@[json: 'xxx']` tag 显式声明序列化字段名，确保与 JS 侧契约稳定。
- JSON 反序列化一律采用 `json.decode(Type, raw) or { Type{} }` 的容错模式，对空串/undefined/null 先做 trim_space 判断再 fallback 到零值结构体。
- HTTP 头取值统一用 `frame.headers[key] or { '' }` 的可选链写法，避免 panic 并保证空串回退。
- 通过 `InProcVjsxExecutor` 方法接收 `VjsxExecutionLane` 参数来注入运行时上下文（provider/kind/config），而非全局变量或闭包捕获。