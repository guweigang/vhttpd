Go 编写的 V8 宿主层，通过 `host.call_entry` 直接调用 JS 入口函数名（`websocket_upstream`、`websocket_affinity`、`websocket_actor`）及全局辅助函数（`__vhttpd_create_runtime`、`__vhttpd_create_websocket_upstream_frame`、`__vhttpd_normalize_websocket_upstream_result`），并以 JSON 作为 Go ↔ JS 的数据交换格式。