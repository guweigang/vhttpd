- Ubuntu 一键安装：`bash scripts/install_ubuntu_from_source.sh`（可通过 `VHTTPD_REPO`/`V_BRANCH`/`V_ROOT`/`WITH_DB`/`RUN_TESTS` 等环境变量定制）
- 主机回归基准：`bash bench/run_host_regression.sh`（可设 `PORT`/`POOL_SIZE`/`RUN_K6=0` 跳过 k6）
- E2E 冒烟测试：`bash tests/e2e/run.sh`（需先 `make build` 生成 `./vhttpd`）