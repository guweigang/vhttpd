- 每个适配器采用「struct + 同名构造函数 + 接收者方法」三件套形式实现 EgressAdapter 接口，构造函数负责字段默认值与参数校验。
- deliver 方法一律通过调用 adapter.v 中的 outcome_* 工厂函数构造 DeliveryOutcome，禁止直接拼装结构体字面量。
- 对外传入的 map[string]string（headers/metadata）在保存前统一使用 .clone() 避免共享可变状态。
- 未使用的参数通过 `_ = ...` 显式忽略，保持 warmup/deliver/close 签名一致且编译无警告。