- TS 模块采用工厂函数模式（如 `createBotApp`, `createStateHelpers`）进行依赖注入，避免全局状态耦合。
- 所有外部协议帧（Feishu/Codex）均通过专用的 `parse*` 函数（如 `parseFeishuInboundFrame`）转换为内部标准化对象后再进入业务逻辑。
- 状态变更统一通过 `state.mjs` 中的原子操作（如 `ensureChatState`, `updateStreamState`）完成，确保 SQLite 事务一致性。
- 命令路由采用谓词匹配链（Predicate Chain），在 `command-router.mjs` 中按优先级顺序匹配 `/help`, `/codex` 等指令前缀。